const TOKEN = "token";
const HEADERS = {
  "Content-Type": "application/json",
  redirect: "follow",
  mode: "no-cors",
  "Access-Control-Allow-Origin": "http://localhost:3000",
  "Access-Control-Allow-Credentials": "true",
};

const USERDETAILS = "user";

export { TOKEN, HEADERS, USERDETAILS };
